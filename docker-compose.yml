services:
  app:
    build: .
    container_name: dml_lms
    restart: unless-stopped
    ports:
      - "8082:80"
    env_file:
      - .env
    networks:
      - shared_network

networks:
  shared_network:
    external: true